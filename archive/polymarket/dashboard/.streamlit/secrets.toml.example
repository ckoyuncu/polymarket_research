# Streamlit Secrets Template
# Copy to secrets.toml and fill in values
# For Streamlit Cloud, add these in the app settings

# SSH connection to Tokyo bot server
SSH_HOST = "18.183.215.121"
SSH_USER = "ubuntu"
SSH_KEY_PATH = "~/.ssh/polymarket-bot-key-tokyo.pem"
REMOTE_PATH = "/home/ubuntu/polymarket_starter"

# Note: For Streamlit Cloud deployment, you'll need to:
# 1. Store the SSH private key content as a secret
# 2. Write it to a temp file at runtime
# See dashboard.py for implementation details
